# 安全策略

GodotX 可以读取和修改项目、执行受控命令并运行游戏代码。安全问题可能直接影响开发者工作区，请不要在公开 Issue 中披露可利用细节。

## 支持版本

项目当前处于 `0.x` 功能预览阶段。安全修复只保证合入最新主分支和最新发布版本；旧预览版本可能不会单独回补。

## 报告漏洞

请使用 GitHub 仓库的 **Security > Report a vulnerability** 创建私有安全报告。报告应包含：

- 受影响版本或提交。
- 运行环境。
- 问题类型和影响。
- 最小复现步骤或概念验证。
- 攻击需要的前置条件。
- 你建议的缓解方案。

请勿附带真实 API Key、私人项目源码或未经脱敏的用户目录。如果 GitHub 私有漏洞报告尚未启用，请先联系仓库维护者启用，而不要提交公开 Issue。

## 主要安全边界

### 本地传输

- Runtime 只监听回环地址。
- 每次插件启动使用新的随机能力令牌。
- WebSocket 升级必须验证能力令牌。
- 编辑器验证 Runtime 返回的工作区和协议版本。
- 编辑器断开后，空闲 Runtime 会在宽限期后退出。

### Provider 连接

- 远程 Base URL 必须使用 HTTPS。
- HTTP 只允许准确的本地回环地址。
- URL 中的用户信息、查询参数和片段会被拒绝。
- API Key 不进入进程参数、项目文件、会话快照或日志。
- Provider 错误在显示前分类和脱敏。

启用“记住密钥”后，密钥会以明文保存到 Godot 用户级 `EditorSettings`，位于仓库外，并按项目和 Provider 哈希隔离。使用共享账户或不可信机器时不要启用。

### 工作区

- 拒绝绝对路径、`..`、外部符号链接和边界逃逸。
- 保护 `.git`、`.godot`、`.godetx`、`.env` 和常见私钥文件。
- 跳过依赖、缓存、虚拟环境和临时目录。
- 文件写入前重新核验 SHA-256 基础状态。
- 一批文件事务必须全部成功，否则回滚。

### 场景编辑

- EditorBridge 只能访问任务提交时冻结的已打开场景。
- 租约绑定会话、任务、场景实例、路径和 Undo 修订。
- 模型不能替换租约 ID、场景路径或修订。
- 场景写入需要专用审批和一次性操作授权。
- 每批实时修改形成可撤销且未保存的编辑器动作。
- 受保护的 owner、script、name 和场景身份属性不能通过通用属性操作修改。

### 命令执行

- 命令不经过 Shell。
- 只允许运行白名单可执行程序。
- 命令受工作区和参数校验约束。
- 直接启动 `godot`、`godot4` 或 Godot 可执行路径会被拒绝。
- `替我审批` 不会取消命令白名单或工作区限制。

### 游戏与自动化

- 启动游戏是独立审批类别。
- 只能停止由当前 GodotX debugger 实例拥有且握手匹配的 `run_id`。
- 手动游戏运行不会被接管。
- 模拟自动化默认关闭，并在任务提交时冻结开关状态。
- 自动化只支持有界等待、Control 点击、InputMap 动作和属性断言。
- 自动化不能调用任意方法、遍历当前场景外节点或修改项目脚本。

### 图片与网页

- 附件和图片响应有数量、尺寸、格式和解码后内存上限。
- 图片 Base64 不通过 WebSocket，也不写入会话 JSON。
- Provider 返回的图片 URL 必须是无凭据 HTTPS，并拒绝明显私有或回环目标。
- 网页工具拒绝私有地址、不安全重定向、非文本响应和过大正文。
- 项目源码、图片批注和网页正文都作为不可信模型输入处理。

## 不属于漏洞的情况

以下情况通常属于已知信任边界或产品限制：

- 用户主动启用“替我审批”后，已知审批类别自动通过。
- 用户允许的模型生成质量不佳或给出错误建议。
- 运行项目自身代码造成的项目既有副作用。
- 将 API Key 主动粘贴到聊天内容后，无法保证启发式脱敏识别所有格式。
- 本地管理员或同一用户账户读取 Godot `EditorSettings` 中已选择持久化的密钥。

如果这些行为能够绕过明确的工作区、审批、命令、租约或凭据边界，仍应作为安全问题私下报告。
